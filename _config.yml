
title:        Static Organization Registry

# Urls
baseurl:      "/registry-org"
repository:   "hasnaetu/registry-org"
repo:         "https://www.github.com/hasnaetu/registry-org"

# People and Social
maintainer: "@hasnaetu"
twitter: hasnaetu

# Registry Metadata
keywords: "singularity, containers"
description: "singularity container and workflow management"

# Defaults
defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
  -
    scope:
      path: "" 
      type: "pages"
    values:
      permalink: "/:path/"

#Others
markdown: kramdown

exclude: [_templates]
